apiVersion: v1
kind: Service
metadata:
  name: ebs-metrics-exporter
  namespace: openshift-sre-ebs-metrics
  annotations:
    package-operator.run/phase: deploy
    # No collision-protection: PKO manages with controller ownership
    # Service will be restored if deleted or edited
    service.beta.openshift.io/serving-cert-secret-name: ebs-metrics-exporter-tls
  labels:
    app.kubernetes.io/name: ebs-metrics-exporter
    app.kubernetes.io/component: metrics-exporter
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: ebs-metrics-exporter
    app.kubernetes.io/component: metrics-exporter
  ports:
  - name: metrics
    port: 8090
    targetPort: metrics
    protocol: TCP
